# ── 비밀·환경 (절대 커밋 금지) ──
.env
.env.local
.env.*
!.env.example
.envrc
*.pem
*.key

# ── Python ──
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Web app / Node
node_modules/
.next/
out/
.vercel/
.turbo/
coverage/

# ── MCP / 런타임 캐시 (설계서 §6-3) ──
data/cache/*
!data/cache/.gitkeep

# ── OS / IDE ──
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
*.swp
*.swo
~$*.pptx
~$*.docx
~$*.xlsx

# ── 로그·임시 ──
*.log
tmp/
temp/
_tmp_*
rawdata/handbook/_probe*.txt

# ── 데이터 수집 파이프라인 (로컬 전용 — 공개 레포 제외) ──
# 크롤러·파서: 원본 데이터 확보 도구 (배포 불필요)
/crawl_alio.py
/parse_alio.py
/parse_handbook.py
# 원본 raw 데이터 (hwpx·pdf·xls·html 등 — 대량, 재수집 가능)
rawdata/
# 크롤·빌드 중간 산출물 (런타임/스냅샷 미사용, 재생성 가능)
data/crawl/
data/items.json
data/items_catalog.json
data/metrics/_crawl_promotion_report.json
